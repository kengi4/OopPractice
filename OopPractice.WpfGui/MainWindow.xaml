<Window x:Class="OopPractice.WpfGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OopPractice.WpfGui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="#1E1E1E">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="LogBox"
             Grid.Column="0"
             Margin="10"
             Background="Black"
             Foreground="#00FF00"
             FontFamily="Consolas"
             FontSize="14"
             IsReadOnly="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Column="1" Margin="10">
            <Label Content="Game Controls" Foreground="White" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Button x:Name="BtnStart" Content="Start New Game" Height="40" Margin="0,5" Click="BtnStart_Click" />
            <Button x:Name="BtnNextTurn" Content="⚔️ Next Turn" Height="50" Margin="0,5" Background="#FF5722" Foreground="White" FontWeight="Bold" Click="BtnNextTurn_Click" IsEnabled="False"/>

            <Separator Margin="0,20"/>

            <Button x:Name="BtnSave" Content="💾 Save Game" Height="30" Margin="0,5" Click="BtnSave_Click" />
            <Button x:Name="BtnLoad" Content="📂 Load Game" Height="30" Margin="0,5" Click="BtnLoad_Click" />

            <Separator Margin="0,20"/>
            <TextBlock Text="Status:" Foreground="Gray"/>
            <TextBlock x:Name="StatusText" Text="Waiting..." Foreground="White" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
